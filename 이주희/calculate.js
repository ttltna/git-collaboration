// Add (덧셈)
const add = (a, b) => a + b
console.log(add(10, 5))

// Subtract (뺄셈)
const sub = (a, b) => a - b
console.log(sub(10, 5))

// Multiply (곱셈)
const mul = (a, b) => a * b
console.log(mul(10, 5))

// Divide (나눗셈)
const div = (a, b) => a / b
console.log(div(10, 5))
